# 核心依赖
# torch 2.11 的构建/运行元数据要求 setuptools<82；固定上限避免 pip 重装后冲突
setuptools<82
numpy>=1.21.0
librosa>=0.9.0
sounddevice>=0.4.0
soundfile>=0.12.0
sherpa-onnx>=1.9.0
# 系统级回声消除（包含其他应用的系统输出参考；按平台安装对应 wheel）
aec-audio-processing-fork==1.0.0.dev1; sys_platform == "darwin"
aec-audio-processing-fork==1.0.1.dev1; sys_platform == "win32"

# TTS
# 注意: 包名是 piper-tts，安装后 import 时用 from piper import PiperVoice
# 切勿 pip install piper（那是另一个无关包 pypiper）
emoji>=2.0.0
piper-tts>=1.0.0
onnxruntime>=1.20.0
# JARVIS-V2 MeloTTS ONNX：中英混合文本前处理与 multilingual BERT 特征
transformers>=4.40.0
torch>=2.4
g2p-en>=2.1.0
cn2an>=0.5.22
pypinyin>=0.50.0
jieba>=0.42.1
num2words>=0.5.13
# 金属感语音后处理用：自带跨平台静态 ffmpeg 二进制，免系统安装/PATH
imageio-ffmpeg>=0.4.9
# Edwin 内置屏幕读取：Pillow 截屏 + macOS Vision OCR，不依赖 Homebrew CLI
pillow>=10.0.0
pyobjc-framework-Quartz>=10.0; sys_platform == "darwin"
pyobjc-framework-Vision>=10.0; sys_platform == "darwin"

# Agent 引擎、Skills 与外部桥接共享依赖
# Edwin 模型 Provider、模型能力探针和快速路由（OpenAI-compatible tool calling）
openai>=1.0.0
# Edwin web_search/CDP HTTP、Hermes/OpenClaw HTTP 桥
requests>=2.28.0
# Edwin Chrome CDP 与 OpenClaw WebSocket 桥（import websocket）
websocket-client>=1.6.0
# OpenClaw 设备签名及旧版模型凭据迁移（Ed25519/Fernet）
cryptography>=41.0.0
# 模型 API Key：macOS Keychain / Windows Credential Manager
keyring>=25.0.0
# Edwin 跨平台只读系统健康/进程/磁盘/网络采集，避免依赖 top/df 等系统 CLI
psutil>=5.9.0
